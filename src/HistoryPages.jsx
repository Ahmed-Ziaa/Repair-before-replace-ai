import { useMemo, useState } from 'react'
import { ArrowLeft, CalendarClock, Check, CheckCircle2, ChevronRight, CircleDollarSign, Filter, Leaf, Plus, Search, ShieldAlert, Sparkles, Wrench } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import { AppSidebar, Button, MobileAppHeader } from './components'

const REPAIRS = [
  { id: 'blender', name: 'Kitchen blender', category: 'Home & kitchen', issue: 'Motor making a humming sound', date: 'May 23, 2025', status: 'Repaired', cost: '$12', saved: '$108', color: 'bg-amber-100', icon: '◒', detail: 'Loose connection in the motor housing', time: '18 minutes' },
  { id: 'jacket', name: 'Denim jacket', category: 'Clothing & textiles', issue: 'Broken zipper', date: 'May 18, 2025', status: 'Repaired', cost: '$6', saved: '$74', color: 'bg-blue-100', icon: '◐', detail: 'Worn zipper slider replaced', time: '12 minutes' },
  { id: 'lamp', name: 'Desk lamp', category: 'Home & kitchen', issue: 'Flickering after switch', date: 'May 11, 2025', status: 'Replaced', cost: '$0', saved: '$0', color: 'bg-violet-100', icon: '◑', detail: 'Replacement chosen after safety check', time: '—' },
  { id: 'headphones', name: 'Wireless headphones', category: 'Electronics', issue: 'Left side quieter than right', date: 'April 29, 2025', status: 'Repaired', cost: '$8', saved: '$132', color: 'bg-emerald-100', icon: '◓', detail: 'Blocked speaker mesh cleaned', time: '9 minutes' },
  { id: 'chair', name: 'Dining chair', category: 'Furniture', issue: 'Loose backrest joint', date: 'April 14, 2025', status: 'Repaired', cost: '$4', saved: '$86', color: 'bg-orange-100', icon: '▱', detail: 'Joint tightened and wood glue set', time: '24 minutes' },
]

const REMINDERS = [
  { id: 'filter', name: 'Clean the air purifier filter', item: 'Bedroom air purifier', date: 'Due today', timing: 'Today', status: 'due', icon: '◫', color: 'bg-sky-100' },
  { id: 'boots', name: 'Condition leather boots', item: 'Brown leather boots', date: 'Due in 4 days', timing: 'May 27', status: 'upcoming', icon: '◒', color: 'bg-amber-100' },
  { id: 'bike', name: 'Lubricate bike chain', item: 'City bike', date: 'Completed May 12', timing: 'May 12', status: 'completed', icon: '◌', color: 'bg-emerald-100' },
  { id: 'coffee', name: 'Descale coffee maker', item: 'Drip coffee maker', date: 'Due in 12 days', timing: 'June 4', status: 'upcoming', icon: '◈', color: 'bg-violet-100' },
]

export function HistoryPage() {
  const [query, setQuery] = useState('')
  const [category, setCategory] = useState('All categories')
  const [status, setStatus] = useState('All statuses')
  const categories = ['All categories', ...new Set(REPAIRS.map((item) => item.category))]
  const filtered = useMemo(() => REPAIRS.filter((item) => {
    const matchesQuery = `${item.name} ${item.issue}`.toLowerCase().includes(query.toLowerCase())
    return matchesQuery && (category === 'All categories' || item.category === category) && (status === 'All statuses' || item.status === status)
  }), [query, category, status])

  return <WorkspacePage><div className="mx-auto max-w-5xl px-5 py-8 lg:px-10 lg:py-12"><PageIntro eyebrow="Your workspace" title="Repair history" subtitle="Every useful thing you chose to keep in use." action={<Button to="/diagnose" variant="green"><Sparkles size={16} /> Diagnose an item</Button>} /><HistoryStats /><div className="mt-10 flex flex-col gap-3 lg:flex-row"><label className="relative flex-1"><Search size={17} className="absolute left-4 top-3.5 text-slate-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search your repairs..." className="w-full rounded-xl border border-slate-200 bg-white py-3 pl-11 pr-4 text-sm outline-none transition focus:border-emerald-500 focus:ring-4 focus:ring-emerald-100" /></label><div className="flex flex-wrap gap-2"><Select value={category} options={categories} onChange={setCategory} /><Select value={status} options={['All statuses', 'Repaired', 'Replaced']} onChange={setStatus} /></div></div><div className="mt-8 flex items-center justify-between"><div><h2 className="font-display text-xl font-semibold tracking-tight">All repairs</h2><p className="mt-1 text-xs text-slate-500">{filtered.length} items in your history</p></div><span className="hidden items-center gap-2 text-xs text-slate-400 sm:flex"><Filter size={14} /> Filtered results</span></div><div className="mt-4 overflow-hidden rounded-2xl border border-slate-200 bg-white">{filtered.length ? filtered.map((item) => <HistoryRow key={item.id} item={item} />) : <EmptyHistory onReset={() => { setQuery(''); setCategory('All categories'); setStatus('All statuses') }} />}</div></div></WorkspacePage>
}

export function RepairDetailPage() {
  const { id } = useParams()
  const item = REPAIRS.find((repair) => repair.id === id) || REPAIRS[0]
  return <WorkspacePage><div className="mx-auto max-w-5xl px-5 py-8 lg:px-10 lg:py-12"><Link to="/history" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-500 hover:text-slate-900"><ArrowLeft size={16} /> Back to history</Link><div className="mt-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><div className="flex items-start gap-4"><div className={`grid h-16 w-16 place-items-center rounded-2xl ${item.color} font-display text-3xl text-slate-600`}>{item.icon}</div><div><p className="text-sm font-medium text-emerald-700">{item.category}</p><h1 className="mt-1 font-display text-4xl font-semibold tracking-[-0.05em]">{item.name}</h1><p className="mt-2 text-sm text-slate-500">Diagnosed {item.date}</p></div></div><StatusPill status={item.status} /></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><div className="space-y-6"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-emerald-700">Diagnosis</p><h2 className="mt-2 font-display text-2xl font-semibold">{item.detail}</h2></div><span className="grid h-11 w-11 place-items-center rounded-xl bg-emerald-50 text-emerald-700"><CheckCircle2 size={20} /></span></div><p className="mt-5 text-sm leading-7 text-slate-600">The item was assessed as repairable. The recommended fix addressed the symptom without needing a full replacement.</p><div className="mt-7 rounded-xl bg-[#eef6ec] p-4"><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">Original problem</p><p className="mt-2 text-sm font-medium text-slate-800">{item.issue}</p></div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><SectionHeading eyebrow="Outcome" title="A small fix, a measurable difference" /><div className="mt-6 grid gap-3 sm:grid-cols-3"><MiniStat label="Repair cost" value={item.cost} /><MiniStat label="Estimated saved" value={item.saved} green /><MiniStat label="Time spent" value={item.time} /></div></section></div><div className="space-y-6"><section className="rounded-2xl bg-slate-950 p-6 text-white"><p className="text-xs font-bold uppercase tracking-[0.16em] text-emerald-300">Impact</p><p className="mt-4 font-display text-4xl font-semibold">{item.status === 'Repaired' ? item.saved : '$0'}</p><p className="mt-1 text-sm text-slate-400">money kept in your pocket</p><div className="mt-6 flex items-center gap-3 border-t border-white/10 pt-5"><Leaf size={18} className="text-emerald-400" /><p className="text-xs leading-5 text-slate-400">You helped extend the useful life of this item.</p></div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><SectionHeading eyebrow="Details" title="At a glance" /><dl className="mt-5 space-y-4 text-sm"><Detail label="Status" value={item.status} /><Detail label="Category" value={item.category} /><Detail label="Completed" value={item.date} /></dl></section></div></div></div></WorkspacePage>
}

export function MaintenancePage() {
  const [completed, setCompleted] = useState(() => new Set(REMINDERS.filter((item) => item.status === 'completed').map((item) => item.id)))
  const upcoming = REMINDERS.filter((item) => item.status === 'upcoming' && !completed.has(item.id))
  const due = REMINDERS.filter((item) => item.status === 'due' && !completed.has(item.id))
  const done = REMINDERS.filter((item) => item.status === 'completed' || completed.has(item.id))
  const markComplete = (id) => setCompleted((current) => new Set([...current, id]))
  return <WorkspacePage><div className="mx-auto max-w-5xl px-5 py-8 lg:px-10 lg:py-12"><PageIntro eyebrow="Care calendar" title="Maintenance" subtitle="Small reminders that keep your things working longer." action={<Button variant="green"><Plus size={17} /> Add reminder</Button>} /><div className="mt-10 grid gap-4 sm:grid-cols-3"><ReminderStat label="Due today" value={due.length} tone="amber" icon={ShieldAlert} /><ReminderStat label="Upcoming" value={upcoming.length} tone="green" icon={CalendarClock} /><ReminderStat label="Completed" value={done.length} tone="slate" icon={CheckCircle2} /></div><div className="mt-12 space-y-10"><ReminderSection title="Due now" description="A little attention today can prevent a bigger repair later." items={due} onComplete={markComplete} empty="You’re all caught up for today." /><ReminderSection title="Upcoming" description="Plan ahead and keep maintenance from becoming an emergency." items={upcoming} onComplete={markComplete} empty="No upcoming reminders." /><ReminderSection title="Completed" description="A record of the care you’ve already put in." items={done} onComplete={markComplete} completed /></div></div></WorkspacePage>
}

function ReminderSection({ title, description, items, onComplete, completed, empty }) { return <section><div className="flex flex-col justify-between gap-2 sm:flex-row sm:items-end"><div><h2 className="font-display text-xl font-semibold tracking-tight">{title}</h2><p className="mt-1 text-sm text-slate-500">{description}</p></div><span className="text-xs font-semibold text-slate-400">{items.length} {items.length === 1 ? 'reminder' : 'reminders'}</span></div><div className="mt-4 overflow-hidden rounded-2xl border border-slate-200 bg-white">{items.length ? items.map((item) => <ReminderRow key={item.id} item={item} completed={completed} onComplete={onComplete} />) : <div className="px-5 py-8 text-center text-sm text-slate-400">{empty}</div>}</div></section> }
function ReminderRow({ item, completed, onComplete }) { return <div className="flex flex-col gap-4 border-b border-slate-100 p-5 last:border-0 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className={`grid h-11 w-11 place-items-center rounded-xl ${item.color} font-display text-xl text-slate-600`}>{item.icon}</div><div><p className={`font-semibold ${completed ? 'text-slate-400 line-through' : 'text-slate-800'}`}>{item.name}</p><p className="mt-1 text-xs text-slate-500">{item.item} · {item.date}</p></div></div>{completed ? <span className="inline-flex items-center gap-1.5 self-start rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-semibold text-emerald-700 sm:self-auto"><Check size={13} /> Done</span> : <button onClick={() => onComplete(item.id)} className="inline-flex items-center gap-2 self-start rounded-xl border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-600 hover:border-emerald-300 hover:bg-emerald-50 sm:self-auto">Mark complete <Check size={14} /></button>}</div> }
function HistoryRow({ item }) { return <Link to={`/history/${item.id}`} className="flex flex-col gap-4 border-b border-slate-100 p-5 transition last:border-0 hover:bg-slate-50 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className={`grid h-11 w-11 place-items-center rounded-xl ${item.color} font-display text-xl text-slate-600`}>{item.icon}</div><div><p className="font-semibold text-slate-800">{item.name}</p><p className="mt-1 text-xs text-slate-500">{item.issue} · {item.date}</p></div></div><div className="flex items-center gap-4 pl-0 sm:pl-0"><StatusPill status={item.status} /><span className="hidden text-xs text-slate-400 md:block">Saved {item.saved}</span><ChevronRight size={16} className="text-slate-300" /></div></Link> }
function StatusPill({ status }) { const replaced = status === 'Replaced'; return <span className={`inline-flex items-center gap-1.5 rounded-full px-3 py-1.5 text-xs font-semibold ${replaced ? 'bg-slate-100 text-slate-600' : 'bg-emerald-50 text-emerald-700'}`}><span className={`h-1.5 w-1.5 rounded-full ${replaced ? 'bg-slate-400' : 'bg-emerald-500'}`} />{status}</span> }
function HistoryStats() { return <div className="mt-10 grid gap-4 sm:grid-cols-3"><StatTile label="Items saved" value="4" icon={Wrench} /><StatTile label="Money saved" value="$400" icon={CircleDollarSign} green /><StatTile label="Waste avoided" value="18.6 kg" icon={Leaf} /></div> }
function StatTile({ label, value, icon: Icon, green }) { return <div className={`rounded-2xl border p-5 ${green ? 'border-emerald-100 bg-emerald-50' : 'border-slate-200 bg-white'}`}><Icon size={18} className={green ? 'text-emerald-700' : 'text-slate-500'} /><p className="mt-4 font-display text-3xl font-semibold tracking-[-0.05em]">{value}</p><p className="mt-1 text-xs text-slate-500">{label}</p></div> }
function ReminderStat({ label, value, icon: Icon, tone }) { const styles = tone === 'amber' ? 'bg-amber-50 text-amber-800' : tone === 'green' ? 'bg-emerald-50 text-emerald-800' : 'bg-white text-slate-800 border border-slate-200'; return <div className={`rounded-2xl p-5 ${styles}`}><Icon size={18} /><p className="mt-4 font-display text-3xl font-semibold">{value}</p><p className="mt-1 text-xs opacity-70">{label}</p></div> }
function PageIntro({ eyebrow, title, subtitle, action }) { return <div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-emerald-700">{eyebrow}</p><h1 className="mt-2 font-display text-4xl font-semibold tracking-[-0.05em]">{title}</h1><p className="mt-2 text-sm text-slate-500">{subtitle}</p></div>{action}</div> }
function SectionHeading({ eyebrow, title }) { return <div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-emerald-700">{eyebrow}</p><h2 className="mt-2 font-display text-xl font-semibold tracking-tight">{title}</h2></div> }
function MiniStat({ label, value, green }) { return <div className={`rounded-xl p-4 ${green ? 'bg-emerald-50' : 'bg-slate-50'}`}><p className="text-xs text-slate-500">{label}</p><p className={`mt-2 font-display text-xl font-semibold ${green ? 'text-emerald-700' : 'text-slate-800'}`}>{value}</p></div> }
function Detail({ label, value }) { return <div className="flex items-center justify-between border-b border-slate-100 pb-3 last:border-0 last:pb-0"><dt className="text-xs text-slate-500">{label}</dt><dd className="text-sm font-semibold text-slate-800">{value}</dd></div> }
function Select({ value, options, onChange }) { return <select value={value} onChange={(event) => onChange(event.target.value)} className="rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm text-slate-600 outline-none focus:border-emerald-500 focus:ring-4 focus:ring-emerald-100">{options.map((option) => <option key={option}>{option}</option>)}</select> }
function EmptyHistory({ onReset }) { return <div className="px-6 py-14 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-slate-100 text-slate-400"><Search size={20} /></div><p className="mt-4 font-semibold text-slate-700">No repairs found</p><p className="mt-1 text-sm text-slate-500">Try a different search or clear your filters.</p><button onClick={onReset} className="mt-4 text-sm font-semibold text-emerald-700">Clear filters</button></div> }
function WorkspacePage({ children }) { return <div className="min-h-screen bg-[#f7f9f6] text-slate-950"><AppSidebar /><div className="lg:pl-60"><MobileAppHeader />{children}</div></div> }
